// Performs the specified action for each element in an array.but don't return anything
const numbers = [1,5,6,4,15];
// const result = numbers.forEach(n=>console.log(n));
const result = numbers.forEach(n=>{

})
console.log(result);

